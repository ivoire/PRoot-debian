Install proot into $(DESTDIR)/usr/bin/
--- a/src/GNUmakefile
+++ b/src/GNUmakefile
@@ -136,7 +136,7 @@
 	-$(RM) -f $(CHECK_OBJECTS) $(CHECK_PROGRAMS) $(CHECK_RESULTS) $(OBJECTS) proot $(DEPS) build.h
 
 install: proot
-	$($(quiet)INSTALL) -D $< $(DESTDIR)/$<
+	$($(quiet)INSTALL) -D $< $(DESTDIR)/usr/bin/$<
 
 uninstall:
-	-$(RM) -f $(DESTDIR)/proot
+	-$(RM) -f $(DESTDIR)/usr/bin/proot
